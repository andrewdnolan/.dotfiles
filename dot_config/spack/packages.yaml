# Spack configuration for packages needing system compilation
# This is used on systems where prebuilt binaries have incompatible GLIBC versions

packages:
  neovim:
    version: [0.11]
    variants: ~treesitter
    compiler: gcc@11
    target: [x86_64]
  all:
    target: [x86_64]
